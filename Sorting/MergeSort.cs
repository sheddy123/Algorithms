using System;
using System.Collections.Generic;
using System.Text;

namespace Sorting
{
    public class MergeSort
    {
        public int[] MergeSortImp(int[] Array, int left, int right)
        {
            int mid = (left + right) / 2;
            if (left < right)
            {
                MergeSortImp(Array[^mid], )
            }
        }
    }
}
